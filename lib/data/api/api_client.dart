/// ApiClient stub — retained for build compatibility.
/// All actual API calls are now made directly (Innertube, Firebase SDK, lrclib.net).
/// This class is intentionally empty. Remove all imports of it when refactoring screens.
class ApiClient {
  static final ApiClient instance = ApiClient._();
  ApiClient._();
}
